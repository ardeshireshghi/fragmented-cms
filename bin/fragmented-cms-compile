#!/usr/bin/env node

const appRoot = require('app-root-path');

const config = require(`${appRoot}/.fragment-cms-config.js`);
const gitFragmentCompiler = require('../src/compiler');

gitFragmentCompiler.compile(config);
